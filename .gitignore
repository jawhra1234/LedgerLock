__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
build/
dist/
data/out/

# Secrets. Never commit a key. .env.example is the committed template.
.env
.env.*
!.env.example

# NOTE: data/llm_cache/ is deliberately NOT ignored. The cached model
# responses are committed so `eval` reproduces the published numbers on a
# clean clone with no API key at all.
